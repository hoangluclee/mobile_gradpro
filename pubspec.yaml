name: doancunhan
description: Ứng dụng quản lý đồ án - khóa luận tốt nghiệp FIT.HUIT
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter

  # Icon
  cupertino_icons: ^1.0.8
  lucide_icons: ^0.257.0
  fluentui_system_icons: ^1.1.258

  # HTTP & API
  http: ^1.2.2
  dio: ^5.9.0

  # Storage
  shared_preferences: ^2.3.2

  # Date & Time
  intl: ^0.19.0

  # Utilities
  url_launcher: ^6.3.0

  # Animations
  animations: ^2.0.11
  flutter_staggered_animations: ^1.1.1
  shimmer: ^3.0.0

  # ==================== FIREBASE & NOTIFICATION ====================
  firebase_core: ^3.6.0
  firebase_messaging: ^15.1.3
  overlay_support: ^2.1.0
  # =================================================================

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/images/huit_logo.png   # Đảm bảo có logo

  